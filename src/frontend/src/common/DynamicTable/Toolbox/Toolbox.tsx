import type React from "react";

interface ToolboxProps {
    filterable: boolean;
    searchable: boolean;
}

const Toolbox: React.FC<ToolboxProps> = ({ filterable, searchable }) => {
    return <></>;
};

export default Toolbox;
